# Archivos y directorios de entorno virtual
rumbita_api/
.venv/
venv/

# Archivos de caché de Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python/

# Archivos específicos del sistema operativo (Windows)
Thumbs.db
Desktop.ini

# Archivos específicos del sistema operativo (macOS) - aunque no es tu caso principal, buena práctica incluir
.DS_Store

# Archivos de logs
*.log
logs/

# Archivos de depuración y temporales
*.swp
*.bak
*.swo
*~
.idea/ # Directorios de configuración de IDEs (ej. IntelliJ, PyCharm)
.vscode/ # Directorios de configuración de VS Code

# Directorios de pruebas (si no contienen código fuente relevante)
.pytest_cache/

# Dependencias generadas (se reinstalan desde requirements.txt)
# Si tu requirements.txt lista todo, no necesitas ignorar más.
# Si instalas dependencias temporales, esas no se deben subir.

# Archivos de bases de datos (si usas SQLite localmente y no quieres versionarlo)
*.sqlite3
*.db